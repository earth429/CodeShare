create table accounts (
  id char(20) primary key,
  salt varchar(40),
  hashed varchar(40),
  algo char(5)
);

create table codes (
  id TEXT primary key,
  username TEXT,
  title TEXT,
  write_time TEXT not null,
  code TEXT not null
);
